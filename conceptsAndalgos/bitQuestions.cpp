#include <iostream>
using namespace std;

int main() {
    // some questions with bit manupulation
    //1) to chk a no. is power of 2 or not
    //  the & of n and n-1 gives the right most set bit
    // so for every power of 2 except 0 we get n & n-1 as 0
    // 2) to cal no. of one's in binary no.
    // we can use the same result as above 
    // we can went on reducing the one's till zero and count
    // how many times we did that ...
    
	return 0;
}

